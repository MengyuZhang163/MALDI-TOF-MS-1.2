# 📦 GitHub上传文件清单

## ✅ 必需上传的文件（5个）

### 1. **app.py** ⭐ 最重要
- **说明**: Streamlit主应用程序
- **作用**: 提供网页界面，调用R脚本处理数据
- **必须上传**: ✅

### 2. **requirements.txt** ⭐ 必需
- **说明**: Python依赖包列表
- **内容**:
  ```
  streamlit==1.31.0
  pandas==2.1.4
  numpy==1.26.3
  openpyxl==3.1.2
  ```
- **作用**: Streamlit Cloud会自动安装这些Python包
- **必须上传**: ✅

### 3. **packages.txt** ⭐ 必需
- **说明**: 系统级依赖包列表
- **内容**:
  ```
  r-base
  r-base-dev
  ```
- **作用**: 安装R语言环境
- **必须上传**: ✅

### 4. **install_r_packages.R** ⭐ 必需
- **说明**: R包安装脚本
- **作用**: 安装MALDIquant、MALDIquantForeign、readxl等R包
- **必须上传**: ✅

### 5. **README.md** 📝 推荐
- **说明**: 项目说明文档
- **作用**: 在GitHub仓库页面显示说明
- **必须上传**: 推荐

---

## 📁 完整的GitHub仓库结构

```
maldi-tof-template/          ← 你的仓库名
│
├── app.py                    ✅ 主程序
├── requirements.txt          ✅ Python依赖
├── packages.txt              ✅ 系统依赖
├── install_r_packages.R      ✅ R包安装
└── README.md                 📝 说明文档
```

---

## 🚀 详细上传步骤

### 方法1: 通过GitHub网页界面上传（推荐新手）

#### 步骤1: 创建GitHub仓库

1. 登录 https://github.com
2. 点击右上角 "+" → "New repository"
3. 填写信息：
   - **Repository name**: `maldi-tof-template`（或其他名字）
   - **Description**: MALDI-TOF MS 模版化处理平台
   - **Public** / Private: 选择Public
   - ✅ 勾选 "Add a README file"
4. 点击 "Create repository"

#### 步骤2: 上传文件

1. 在仓库页面，点击 "Add file" → "Upload files"
2. 拖拽或选择以下文件：
   - `app.py`
   - `requirements.txt`
   - `packages.txt`
   - `install_r_packages.R`
3. 在底部填写提交信息：`Add application files`
4. 点击 "Commit changes"

#### 步骤3: 验证文件

确认仓库中有这些文件：
- ✅ app.py
- ✅ requirements.txt
- ✅ packages.txt
- ✅ install_r_packages.R
- ✅ README.md

---

### 方法2: 通过Git命令行上传（推荐熟悉Git的用户）

```bash
# 1. 创建本地目录
mkdir maldi-tof-template
cd maldi-tof-template

# 2. 初始化Git仓库
git init

# 3. 复制文件到这个目录
cp /path/to/app.py .
cp /path/to/requirements.txt .
cp /path/to/packages.txt .
cp /path/to/install_r_packages.R .
cp /path/to/README.md .

# 4. 添加文件
git add .

# 5. 提交
git commit -m "Initial commit: Add MALDI-TOF MS template processing app"

# 6. 连接到GitHub远程仓库
git remote add origin https://github.com/你的用户名/maldi-tof-template.git

# 7. 推送
git branch -M main
git push -u origin main
```

---

## 🌐 部署到Streamlit Cloud

### 步骤1: 访问Streamlit Cloud

访问: https://share.streamlit.io/

### 步骤2: 登录

使用你的GitHub账号登录

### 步骤3: 创建新应用

1. 点击 "New app"
2. 填写信息：
   - **Repository**: 选择 `你的用户名/maldi-tof-template`
   - **Branch**: `main`
   - **Main file path**: `app.py`
3. 点击 "Deploy!"

### 步骤4: 等待部署

- 首次部署需要5-10分钟（安装R环境）
- 可以在"Logs"中查看进度
- 看到 "App is live!" 表示成功

---

## ⚠️ 重要注意事项

### 1. 文件名大小写

- 文件名必须精确匹配
- `app.py` ✅
- `App.py` ❌
- `APP.py` ❌

### 2. 文件编码

- 所有文件使用 **UTF-8** 编码
- Windows记事本可能使用GBK，需要注意

### 3. 换行符

- 推荐使用 **LF**（Unix风格）
- Windows的CRLF也可以，但可能有警告

### 4. 不要上传的文件

❌ 不要上传：
- `__pycache__/`
- `.DS_Store`
- `.git/`（如果用网页上传）
- 任何临时文件

---

## 🔧 验证清单

上传完成后，检查：

- [ ] ✅ 文件数量：至少4个（app.py, requirements.txt, packages.txt, install_r_packages.R）
- [ ] ✅ 文件名正确：没有大小写错误
- [ ] ✅ 文件内容完整：在GitHub上点击文件可以看到内容
- [ ] ✅ README.md存在且显示正常
- [ ] ✅ 仓库设置为Public（Streamlit Cloud免费版需要Public仓库）

---

## 📞 常见问题

### Q1: 我不会用Git怎么办？

**A**: 使用方法1（GitHub网页界面），非常简单！

### Q2: 部署失败怎么办？

**A**: 检查：
1. 文件是否都上传了
2. 文件名是否正确
3. requirements.txt格式是否正确
4. 查看Streamlit Cloud的日志

### Q3: 可以改仓库名吗？

**A**: 可以！仓库名不影响部署，只要在Streamlit Cloud选择正确的仓库即可。

### Q4: 必须是Public仓库吗？

**A**: Streamlit Cloud免费版需要Public仓库。如果用Private仓库，需要付费订阅。

---

## 🎯 下一步

上传完成后：
1. 访问Streamlit Cloud
2. 部署应用
3. 等待部署完成
4. 开始使用！

---

**祝你部署成功！** 🎉
